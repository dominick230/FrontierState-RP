# Frontier State RP backend foundation

This folder is the production-server starting point for the same game architecture. The Android prototype runs locally, but the full multiplayer version must move money, inventory, permissions, positions, vehicles, and other authoritative state to a server.

The included API contract is intentionally framework-light. It defines the data and endpoints the client will use when an online backend is deployed.
